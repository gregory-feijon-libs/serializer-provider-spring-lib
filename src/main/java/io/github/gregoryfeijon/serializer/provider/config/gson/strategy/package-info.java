/**
 * Gson serialization and exclusion strategies.
 * <p>
 * Provides custom {@link com.google.gson.ExclusionStrategy} implementations
 * for controlling which fields and classes are included during Gson serialization
 * and deserialization.
 */
package io.github.gregoryfeijon.serializer.provider.config.gson.strategy;
